import React from "react";
import ConsultationFormNew from "../components/consultation/ConsultationFormNew";

const Consultation = () => {
  // New form doesn't use Layout wrapper since it has its own full-screen three-panel layout
  return <ConsultationFormNew />;
};

export default Consultation;
